{{- $instance := include "hermes.instance" . }}
{{- $h := .Values.hermes }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $instance }}
  namespace: {{ include "hermes.ns" . }}
  {{- if $h.renderLabels }}
  labels:
    app: {{ $instance }}
    {{- with $h.extraLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  replicas: {{ $h.replicaCount }}
  strategy:
    type: {{ $h.strategy }}
  selector:
    matchLabels:
      app: {{ $instance }}
  template:
    metadata:
      labels:
        app: {{ $instance }}
    spec:
      {{- if $h.serviceAccountName }}
      serviceAccountName: {{ $h.serviceAccountName }}
      {{- end }}
      {{- with $h.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $h.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: hermes-agent
        image: "{{ $h.image.repository }}:{{ $h.image.tag }}"
        imagePullPolicy: {{ $h.image.pullPolicy }}
        args: [gateway, run]
        ports:
        - {containerPort: 8642, name: gateway, protocol: TCP}
        - {containerPort: 9119, name: dashboard, protocol: TCP}
        env:
          {{- toYaml $h.env | nindent 8 }}
        {{- if $h.dashboardAuth.enabled }}
        - name: HERMES_DASHBOARD_BASIC_AUTH_USERNAME
          value: {{ $h.dashboardAuth.username | quote }}
        - name: HERMES_DASHBOARD_BASIC_AUTH_PASSWORD
          value: {{ $h.dashboardAuth.password | quote }}
        {{- end }}
        {{- with $h.extraEnvTail }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if $h.lifecyclePostStart }}
        lifecycle:
          postStart:
            exec:
              command: ["sh", "-c", {{ $h.lifecyclePostStart | quote }}]
        {{- end }}
        {{- if $h.probes.enabled }}
        readinessProbe:
          tcpSocket: {port: 8642}
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 6
        livenessProbe:
          tcpSocket: {port: 8642}
          initialDelaySeconds: 30
          periodSeconds: 30
          failureThreshold: 5
        {{- end }}
        resources:
          {{- toYaml $h.resources | nindent 10 }}
        {{- with $h.containerSecurityContext }}
        securityContext:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        volumeMounts:
        - {mountPath: /opt/data, name: hermes-data}
        {{- if $h.sharedVolumes.enabled }}
        - {mountPath: /shared-pw, name: playwright-shared}
        - {mountPath: /shared-tools, name: tools-shared}
        {{- end }}
      {{- with $h.extraContainers }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      volumes:
      - name: hermes-data
        persistentVolumeClaim:
          claimName: {{ $h.persistence.claimName }}
      {{- if $h.sharedVolumes.enabled }}
      - name: playwright-shared
        emptyDir: {}
      - name: tools-shared
        emptyDir: {}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $instance }}-agent-svc
  namespace: {{ include "hermes.ns" . }}
  {{- if $h.svcRenderLabels }}
  labels:
    app: {{ $instance }}
    {{- with $h.svcExtraLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  selector:
    app: {{ $instance }}
  ports:
  - port: {{ $h.service.gatewayPort }}
    targetPort: 8642
    name: gateway
    protocol: TCP
    {{- if and (eq $h.service.type "NodePort") $h.service.nodePort }}
    nodePort: {{ $h.service.nodePort }}
    {{- end }}
  - port: {{ $h.service.dashboardPort }}
    targetPort: 9119
    name: dashboard
    protocol: TCP
    {{- if and (eq $h.service.type "NodePort") $h.service.dashboardNodePort }}
    nodePort: {{ $h.service.dashboardNodePort }}
    {{- end }}
  type: {{ $h.service.type }}
{{- range $h.extraServices }}
---
{{ toYaml . }}
{{- end }}
{{- if and $h.serviceAccount.create $h.serviceAccountName }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $h.serviceAccountName }}
  namespace: {{ include "hermes.ns" . }}
{{- end }}
---
# Agent data PVC. Name kept overridable (see hermes.persistence.claimName
# comment in values.yaml) for backward compatibility with pre-existing
# volumes from the sidecar era.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $h.persistence.claimName }}
  namespace: {{ include "hermes.ns" . }}
  {{- include "hermes.keepAnnotations" . | nindent 2 }}
spec:
  storageClassName: {{ $h.persistence.storageClassName }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ $h.persistence.size }}
